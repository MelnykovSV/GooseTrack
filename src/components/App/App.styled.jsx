import styled from '@emotion/styled';

const Container = styled.div`
  font-size: 50px;
  font-family: 'Inter';
  font-weight: 700;
  font-style: italic;
  padding: 20px;
`;

export default Container;
